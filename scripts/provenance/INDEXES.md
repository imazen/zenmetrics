# Corpus provenance indexes

Content-addressed indexes for the picker/codec corpora, per the ML Data Pipeline
Discipline. Generated by `scripts/provenance/index_corpus.py`. The TSVs are data
(>30 KB) → they live in block storage + R2, NOT git; this file is the tracked
pointer (path + sha256 + what each indexes).

## renditions_index_2026-06-23.tsv
- **What:** sha256 + bytes + source_stem + dims + size_class + r2_key for every
  rendition in `train_renditions_2026-06-14` (1482 files, 1082 source stems).
- **size_class counts (authoritative):** small 1121 / tiny 161 / **large 122** /
  **medium 78**. Medium is the sparsest — the picker tail-gate bottleneck.
- **Local:** `/mnt/v/output/imazen-26-features/renditions_index_2026-06-23.tsv`
- **R2:** `s3://codec-corpus/picker-sweep-2026-06-22/renditions_index_2026-06-23.tsv`
- **sha256:** `c25cb63c270da1af8f6b1712ab7259ef1a107f0d808962464866ded167936062`
- **Tool:** `index_corpus.py --name-dims --r2-prefix s3://codec-corpus/picker-sweep-2026-06-22/renditions`

## Pending (the rest of the chain)
- **sources_index**: source_stem → original_path → sha256 → dims → collection →
  license. Blocked on the rendition→original mapping (Explore agent hunting it);
  once known, hash the originals and link `renditions_index.source_stem` → original sha.
- **sweep index**: every sweep cell references `encode_sha256 = sha256(encoded_bytes)`
  (content-addressed) so scalar scores rejoin the bitstream forever. Wire into the
  sweep runner so it emits the row at encode time.
- **feature index**: zenanalyze features keyed by rendition sha256 (not just name)
  so they survive renames/regen.
- **canonical DATA_PROVENANCE doc** tying renditions ↔ sources ↔ sweeps ↔ features
  ↔ encoded variants, each with build_commit.

See memory `corpus-provenance-hash-index`.

# Corpus provenance — imazen-26 picker/codec corpus

Content-addressed indexes per the ML Data Pipeline Discipline. Generated by
`scripts/provenance/index_corpus.py` (renditions) + `index_sources.py` (originals).
The TSVs are data (>30 KB) → block storage + R2, NOT git; this file is the tracked
**canonical pointer** (path + sha256 + what each indexes + how they link).

## The chain (VERIFIED complete 2026-06-23)
```
originals (.sdr.png) --[Mitchell+sharpen=10.0 downscale]--> renditions --> sweeps --> features
```
All 1082 rendition `source_stem`s link to a hashed source original — **0 orphans,
0 missing files**. Recipe rendition→original: take the stem (substring before
`.scale`), look it up in the manifest `stem` column, read `original_path`.

## Manifest (the stem→original map)
- `/mnt/v/output/imazen-26-features/imazen26_manifest.tsv` (2157 originals)
- R2: `s3://codec-corpus/picker-sweep-2026-06-22/imazen26_manifest.tsv`
- sha256: `fa36092485eed4afdc09ed340b8f46c8da81200d6659054b3f9a5d34a15ba10c`
- schema: `stem  split  content_class  source  original_path`

## renditions_index_2026-06-23.tsv
- 1482 renditions / 1082 stems. cols: `name sha256 bytes source_stem width height px size_class r2_key`
- size_class: small 1121 / tiny 161 / large 122 / **medium 78** (medium = picker tail bottleneck)
- `/mnt/v/output/imazen-26-features/renditions_index_2026-06-23.tsv` · R2: `.../renditions_index_2026-06-23.tsv`
- sha256: `c25cb63c270da1af8f6b1712ab7259ef1a107f0d808962464866ded167936062` · tool: `index_corpus.py --name-dims`

## sources_index_2026-06-23.tsv
- 2157 originals, **0 missing on disk, 1708 >1 MP** (expansion headroom). cols: `stem sha256 bytes width height px content_class source split original_path`
- `/mnt/v/output/imazen-26-features/sources_index_2026-06-23.tsv` · R2: `.../sources_index_2026-06-23.tsv`
- sha256: `1b8098bea98402a569e4405b16f9726f95433acf1eb59d2e85d7eb984236d80c` · tool: `index_sources.py --manifest imazen26_manifest.tsv`
- originals dir: `/mnt/v/output/imazen-26-png/<content_class>/<stem>_..._.sdr.png`

## Generation tool/kernel (to match the corpus on re-gen)
- `~/work/zen/zenanalyze/examples/render_imazen26_variants.rs` (zenanalyze — rsqrt-locked; read-only)
- zenresize `Filter::Mitchell` + `resize_sharpen(10.0)`; equal source/target dims = pixel copy (no resample)

## Pending (rest of the chain)
- **sweep index**: every sweep cell references `encode_sha256 = sha256(encoded_bytes)` — wire into the sweep runner so it emits the row at encode time (content-addressed = free dedup).
- **feature index**: zenanalyze features keyed by rendition sha256 (survive renames/regen).
- Add a pointer to these from the workspace canonical `~/work/zen/DATA_PROVENANCE.md`.

See memory `corpus-provenance-hash-index`.

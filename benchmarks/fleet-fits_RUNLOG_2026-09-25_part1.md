# fleet-fits run log (2026-09-25/26), part 1 (D1-D18)

Snapshot of the lane's decision log; times are UTC, `Dn` numbers are the lane's decision ids. Household hosts appear under their neutral IDs (`node-2` = 16-thread AVX2 host, `node-3` = 12-thread AVX2 host, `node-5` = 24-core AVX2 host without AVX-512). The workstation VM host that was briefly enrolled and removed is not named.

## Decisions log (newest last; user asked for "latest main, faster, report decisions")

- **12:00Z D1 — latest mains.** Fetched all six repos. zensim main is now `424b8b02` (was `390c99a3`; the delta is `zensim/src/color.rs` scalar-tier XYB fix, not the trainer), zenanalyze `6bbcacb1`, zenmetrics master `9a33f741` (no `zenfleet-*`/`zenstats` change since `8dd8d07c`). All new builds use these heads (zenstats/zenpredict pins moved in the build snapshot).
- **12:02Z D2 — where the time goes (measured).** `perf record` of one H32 training on r3500 (AVX2): `adam_update_inner_v3` 40.7%, `simd_mlp::forward_avx2` 23.9%, `backprop_avx2` 22.1%, `add_l2_grad_layer1` 5.9%. A fit is single-threaded (docker stats 100% per container), so throughput = number of concurrent cells, not threads per cell. The dev box (AVX-512, 32 threads) trains ~3.5x faster per epoch than r3500 (1.5 s vs 5.3 s per epoch) and has far more cores than the whole fleet.
- **12:03Z D3 — use every core we may use first (no numerics change).** node-2 has 16 threads and was running 1 cell; node-3 had 4 of 12 threads busy. Measured node-3: one H32 inner fold takes 280-330 s with 1-4 containers, so I am raising it to 6 containers now and node-2 as soon as its build job finishes. Tower stays at the coordinator's 12-cell cap.
- **12:05Z D4 — a bit-identical AVX2 tier cap so the dev box can run this era.** Added `ZENSIM_MAX_TIER=v3` (zensim-validate `tier_cap.rs`, one call at the top of `zensim_mlp_train`, `bake_dial_refit`, `panel`) which disables the AVX-512 tokens process-wide via archmage's own `dangerously_disable_token_process_wide` (no unsafe). Commit `f678a0f4` on `quarantine/codex/fleet-fits` in the zensim workspace, no push. This is the "separately reviewed tier cap" the GO asked for; it is only used if the capped dev box reproduces the fleet results bit for bit (test pending: fleet gate cell `POT_kadid_train_r0_mlp32/o0_r0`, then the first done production cells). Until that passes the dev box is not used.
- **12:06Z D5 — build off the shared heavy lock.** My builds waited 25+ minutes behind other lanes on the dev box's heavy lock, so builds now run on node-2 in an ubuntu:26.04 container (release+`target-cpu=x86-64-v3`, release, and dev-profile variants, plus the zenfleet worker and ctl). Numerics are not assumed equal across profiles; each variant is checked against the fleet gate result before use.
- **12:12Z D6 — release-profile fit binaries (measured, bit-identical).** The fleet binaries were the `dev` profile (opt-level 2, no LTO). A `--release` build (opt 3, thin LTO, cgu 1) runs the same fold **1.64x faster** on r3500 (H32, 8 epochs: 46.1 s -> 28.2 s; startup included) and produces byte-identical weights and identical epoch lines (all three builds: dev, release, release+`target-cpu=x86-64-v3`; v3 adds nothing, so the generic release build is used). New images `fit-p0-v7` / `fit-p2d2-v7` carry them; a full-cell identity gate against the v5 gate result is running on node-2 (and capped on the dev box, queued on the heavy lock).
- **12:20Z D7 — the tier cap needed two hooks, now verified.** Disabling the archmage AVX-512 tokens was not enough: `simd_mlp.rs` picks `forward_avx512`/`backprop_avx512` with `is_x86_feature_detected!("avx512f")`. `tier_cap::avx512_allowed()` now guards those two sites (commit `83461101`). Result on the dev box (Zen5) with `ZENSIM_MAX_TIER=v3`: epoch lines and weight bytes **identical** to the AVX2 golden captured on r3500 (`~/tmp/devin/fitopt/bitexact.sh`: `epoch lines identical: True | weights identical: True`); without the cap it prints 0.9578 (AVX-512) instead of 0.9587. The dev box trains this fold in 6.3 s for 3 epochs including load (r3500: 11.3 s), i.e. about 2x per core, with 32 threads. It is NOT yet used as a worker: the coordinator's GO excluded it pending review; this is the evidence for that review (full-cell identity gate queued).
- **12:23Z D8 — parallel optimization lanes (free SWE-2 agents in herdr), bit-identical only.** `optadam` (owns `adam_simd.rs`, the 41% hotspot) started; `optmlp` (owns `simd_mlp.rs` AVX2 forward/backprop and `add_l2_grad_layer1`, 52% together) starts next. Oracle: existing equivalence tests + `bitexact.sh` (epoch lines and weight bytes). Tools they must use: iai-callgrind (instruction counts, immune to timing noise), cargo-show-asm, archmage/magetypes. Nothing lands in the fleet image without the gate cell reproducing the v5 result.
- **Isolation between parallel trainings (answer to "avoid contamination").** Each cell already runs in its own container with: its own scratch dir, its own `/var/tmp/rev4-featpot` symlink view, a read-only verified data archive, per-process seeds derived only from the cell's argv (`--init-seed`, `--sample-seed`), `RAYON_NUM_THREADS` pinned, one core each. Evidence of no cross-talk: the same cell gives bit-identical results solo on r3500 and among 12 concurrent cells on the tower, and across runs on different hosts. For any native (non-container) run: unique output dir per cell, no shared cache, `ZENSIM_MAX_TIER=v3`, `taskset` to one core. Shared mutable state is the only real risk, and there is none between containers.
- **13:00Z D9 — coordinator's throughput sweep applied.** Coordinator measured system-wide instructions/s with `docker pause` subsets: node-3 71.2 G/s at 6 cells, 62.9 at 8, 48.1 at 10, 47.6 at 11 (SMT oversubscription on DDR4-2133 costs 33%); node-2 57.8 G/s at 11, 64.5 at 13, 71.4 at 15; r3500 flat at 67 G/s for 5 to 6. New targets node-3 6, node-2 16, r3500 6. Done at 13:00Z: **node-3 11 -> 6**, **node-2 15 -> 16**, r3500 stays 6, tower stays 22 (coordinator measuring it next).
  - *How node-3 was reduced without wasted work:* the worker's drain is jobset-wide (`control.json`), not per container, so a per-container drain does not exist. I stopped the 5 youngest containers (their cells had run 11 minutes each: v7-v11) and **released their 5 claims by deleting the claim objects** (68 -> 63 claim objects), so the cells are re-claimable immediately; no ledger row existed for them.
  - *Claim TTL I set:* `ZEN_CLAIM_TTL_SECS=21600` (6 h, the R2 lease age after which another worker may steal a chunk) and `ZEN_STALE_CLAIM_SEC=21600` (the ledger-side stale-claim age); the worker default would have been 600 s / 1800 s. Only completions renew a lease, so 6 h must exceed one cell's wall time on the slowest host (H128, D2 cells need more: 24 h for D2 jobsets). A container that dies mid-cell therefore holds its chunk for up to 6 h unless the claim object is deleted, as done here.
- **13:14Z D10 — tower corrected to 10 active (coordinator's sweep: 22 cells 27.6 G instr/s, 20 31.7, 18 37.6, 16 42.3, 14 52.8, 12 64.0, 10 66.0, 8 58.5, 6 49.1, 4 34.2; memory-bandwidth bound, 19 of 22 cells were H128).** The coordinator paused t11-t22 (docker pause, no work lost). I chose option (a), rotation, implemented as `/var/tmp/fleet-fits/tower_rotator.py` (log `tower_rotator.log`): it keeps at most 10 tower containers unpaused; whenever an active container finishes its cell and starts a fresh one (detected by the running `mlp_probe.py` argv changing), it removes that container, deletes the fresh claim it just took (<1 min of progress, released so any worker can take it) and unpauses one frozen container, oldest first (t11, t12 hold ~3 h of progress). If anything unpauses everything again (e.g. a sweep script's exit trap) it pauses the extras with the least progress. When no frozen container is left it stops and the remaining 10 keep claiming normally. Nothing is discarded except sub-minute starts.
  - *Lease TTL:* claim leases are `ZEN_CLAIM_TTL_SECS=21600` (6 h) on every worker; only completions renew a lease, and a frozen container cannot renew, so the rotator **rewrites each frozen container's claim object with a fresh timestamp every 30 minutes** (`<epoch> <worker> 0/1`, the same format the worker's own renewal writes; done once at 13:14Z for t11-t22, whose oldest leases dated from 10:26Z and would have expired at 16:26Z). The unfrozen ones need no refresh: every one finishes and completes well inside 6 h.
  - Final targets recorded: tower 10, r3500 6, node-3 6, node-2 16. node-2 re-measurement at 16 with `fleet_sweep.sh` is running (levels 16, 14, 12, 16); result below when it finishes.
- **13:19Z D11 — node-2 re-measured at 16 (coordinator's `fleet_sweep.sh`, levels 16/14/12/16, mix 10 H32 + 6 H128):** 61.6, 61.5, 56.1, 66.3 G instr/s. The two 16-cell readings bracket the 14-cell one (noise ~±4 G/s between passes, the sweep pauses and unpauses production containers); 12 cells is clearly lower. node-2 stays at 16 (all unpaused again after the sweep: 0 paused). The earlier coordinator figure at 15 (71.4) used a different H32/H128 mix.
- **13:25Z D12 — release-profile identity gate PASSED on node-2 (Raptor Lake, AVX2).** Full cell `POT_kadid_train_r0_mlp32/o0_r0` (incl. the importance step) run through the release-binary image on node-2 vs the v5 gate results (dev-profile binaries; Zen+ and Zen2): all required fields equal (prediction, score, selected epoch, inner series, seeds, bounds, checkpoint weights) and the 6 permutation-importance SROCCs equal; SROCC 0.9499134326779027 on all three. The gate's weight hash now masks the two metadata-length header fields (they changed with source-path strings only). The corrected images (`fit-p0-v7`, program sha `51e2debf…`, with the simd_mlp tier-cap hook) differ from the tested node-2 image only by that hook, which is inert without `ZENSIM_MAX_TIER`; the capped dev-box run of the corrected image is still queued on the heavy lock.
- **13:22Z D13 — P0 moves to the release-profile image (bit-identical, ~1.6x faster per cell).** Pushed `ghcr.io/imazen/zenfleet-worker:fit-p0-v7` (registry digest `sha256:95ceb0b13b083e8d1d7fec19c1a03b98766f0d18c2f5d328d2e2cc856121f284`, program sha `51e2debf…`) and `fit-p2d2-v7` (`sha256:bd67155308306f9f7e3eeba361892e807885ebcf6adc872277580a75c9351c84`, program sha `d1a4a1a2…`). Evidence for bit-identity: full-cell gate on node-2 (D12) plus the corrected binary reproducing the AVX2 golden uncapped on r3500 (epoch lines and weight bytes equal). New jobset `fitp0v7-20260925` = the 893 cells that were neither done nor claimed in `fitp0-20260925` (matched by destination; claims of single-cell chunks are `chunk-sha256(job_id+"\n")`), same data archive sha `5db2419e…`. The old jobset is **draining** (control.json `drain:true`, 13:22Z): its ~50 running/frozen cells finish, nothing new is claimed. As each old container exits, a v7 container takes its slot: `handoff.sh` for r3500 / node-3 / node-2 (slot counts 6 / 6 / 16), `tower_rotator.py` v2 for the tower (unpause a frozen old container first, otherwise start a new v7 container; never more than 10 active). Both jobsets are harvested; cells are one era because outputs are bit-identical.
- **13:46Z D14 — coordinator's three v7-era conditions, execution state.** Until (1) and (2) pass, v7 cells are **not** counted as the same era; the handoff of drained containers to v7 was already running, so these v7 cells are held aside (their jobset is harvested only after the gates pass).
  1. *H128 P0 full-cell gate (importance included), v6 dev-profile vs v7 release on the same AVX2 host:* cell `POT_aic3_minus_basic_mlp128/o0_r0` run under `fit-p0-v6` and `fit-p0-v7` on node-3 (containers `gate-p0v6`, `gate-p0v7`, cpu-shares 512 so production keeps priority; started 13:45Z); the v6 production result of the same cell (from r3500, 13:xx) is a third comparator. Result: pending.
  2. *`fit-p2d2-v7` (program `d1a4a1a2`) vs the dev-profile build of the same program (`fit-p2d2-dev`, program `3ffd4a55`, image built from the dev-profile binaries) on the same host (node-3):* P2 H32 `POT_kadid_train_p2_mlp32/o0_r0`, D2 LODO `LODO_p2_mlp32/without_aic3_r0` (both in containers `gate-p2devA` / `gate-p2v7A`), P2 H128 `POT_aic3_p2_mlp128/o0_r0` (`gate-p2devB` / `gate-p2v7B`). Result: pending. No P2/D2 jobset has been declared or claimed.
  3. *Standing guard:* stratified sample of 9 finished v6-era P0 cells (+ the H128 gate cell = 10; 1+ per set for aic3, cid22_a25, kadid_select, konfig_train, konfig_val, tid2013, both widths) rerun under v7 and compared bit for bit (`guard_compare.py`: required fields incl. weights, importance permuted SROCCs). It starts when node-3 has free threads (after the gates); kadid_train and konjnd_bpg_train cells are added when they land. Any mismatch stops v7 claiming.
  - *Dev box:* its capped full-cell gate (H32 and H128) is queued on the heavy lock; the joining proposal follows once it passes. Optimization lanes: same gate, H128 included, new binary = new image = new gate (noted in their briefs' report requirements).
- **14:35Z D15 — correction: the drain flag does not retire running workers.** The old jobset's `control.json` `drain:true` (13:22Z) stops new *passes* from claiming, but a running worker pass keeps claiming its next chunk (measured: `node-2-u4` finished a cell at 14:30:30 and 3 minutes later was running a fresh old-jobset cell). No key was claimed by a new pass, and the `handoff.sh` trigger (container exit) therefore never fired; I stopped it. Replacement: `finisher.py` (log `finisher.log`) retires old containers by artifact: when a worker's per-chunk ledger sidecar appears (its cell is done and persisted) it removes that container, deletes the fresh claim the worker just took (seconds of progress; released so any worker can take it) and starts a v7 container in that slot on the new jobset (tower: `tower_rotator.py` refills the slot, frozen containers first). Every old-jobset cell that was already running still finishes; the cost is one aborted just-started cell per container, seconds each. Until the v7-era gates pass, v7 cells are held aside (harvested only after the gates).
- **16:02Z D16 — gate results so far (node-3 gates paused production; leases kept alive).** Production containers on node-3 (v1-v6) were `docker pause`d at 15:59Z; `pause_refresher.py node-3` rewrites their claim timestamps every 30 min (first refresh 16:00Z; TTL 6 h; their oldest claims dated 10:56Z). Unpause + standing-guard start the moment the 5 remaining gate containers exit. Results:
  - **Dev box, tier-capped, corrected `fit-p0-v7`, P0 H128 full cell (importance included) `POT_aic3_minus_basic_mlp128/o0_r0` vs the v6 production result of the same cell (dev-profile binaries, r3500/Zen2): ALL EQUAL** (result.json minus runtime/path fields, checkpoint weight bytes, test predictions, permutation importances). The earlier H32 dev-capped gate (`o0_r0`) was also ALL EQUAL vs the v5 gate on tower and r3500. Neither dev-box gate had to wait: both finished by 14:33Z (no heavy-lock queue left).
  - **`fit-p2d2-v7` on node-3 vs the tier-capped dev box, same program:** P2 H32 `POT_kadid_train_p2_mlp32/o0_r0` **ALL EQUAL** (result, weights, predictions, all 7 importance families incl. `peer_gmsd_gmsm`), D2 LODO `LODO_p2_mlp32/without_aic3_r0` **ALL EQUAL** (result, weights, eval predictions). The dev-profile-vs-v7 same-host comparisons (`gate-p2devA/B`, `gate-p0v6`) are still running on node-3 (`gate-p2v7A` finished first; the H128 ones run alone now).
- **16:05Z D17 — orphaned claims found and fixed.** `finisher.py` used a timezone-shifted timestamp filter (S3 listings are UTC; I subtracted the local zone offset), so the "fresh claim" it should delete right after retiring a container was never found: 13 orphan claims (old jobset) held by removed workers, each ~6 h from expiry. Released all 13 at 16:00Z (cells become claimable again); fixed the filter (`calendar.timegm`), restarted the finisher. **Open item:** those released cells lie outside the new jobset's manifest (it excluded everything claimed in the old jobset) — a leftover jobset `fitp0v7b` will be declared for cells that end up neither done nor in either manifest, after the old containers have all retired.
- **16:08Z D18 — proposal: the dev box joins as a tier-capped worker (awaiting the coordinator's OK; nothing started).** Gates passed: capped H32 and H128 P0 cells and P2 H32 / D2 cells bit-identical to fleet results (D16). Proposed:
  - *Image and cap:* `fit-p0-v7` / `fit-p2d2-v7` with `ZENSIM_MAX_TIER=v3` (the container env; a container without it would silently produce AVX-512 numbers, so the launcher refuses to start without it and each cell's `selected_epoch`/weights are spot-checked by the standing guard).
  - *Slots:* start with 8 containers pinned to cpuset 24-31 (one CCD's SMT-free set is not known; the coordinator's sweep decides), `--memory 2g` each (0.25-0.4 GB per cell measured), `--cpu-shares 128` and `nice 19` so any other lane's job wins the CPU. Then measure with `fleet_sweep.sh` (4/8/12/16 cells) and set the plateau, expected near the other hosts' 65-70 G instr/s memory-bound limit; more than that only steals bandwidth from other lanes.
  - *Heavy lock:* the containers do not go through `run-heavy` (a multi-day worker would hold the lock and block every other lane). Compensation: fixed memory ceiling (8 x 2 GiB = 16 GiB, under the 24 GiB default `run-heavy` cap and far below the 58 GiB box), idle CPU priority, scratch under `/var/tmp/fleet-fits/dev-scratch`, and an automatic pause/unpause guard: all dev-box containers are `docker pause`d while `MemAvailable` < 10 GiB or the coordinator/other lanes create `~/tmp/devin/DEVBOX_PAUSE`; unpaused when it clears (leases refreshed every 30 min by the same refresher, 6 h TTL).
  - *Which work:* the P2/D2 jobsets first (they are the largest remaining and the dev box is the only host with the headroom); P0 v7 cells continue on the LAN hosts.

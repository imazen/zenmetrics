# fleet-fits worklog: predecessor lane (2026-09-24, until the quota stop)

Split from `fleet-fits_WORKLOG.md` (which continues from the Sonnet takeover). Original text unchanged below.

UTC, 2026-09-24. This lane follows `FLEET_FITS_brief.md`, `DEVIN_COMMON.md`, `CODEX_NOTE.md`, the Rev4 program and the potential preregistration. Scope is fleet transport and deterministic replay, not statistical selection. Work is quarantined on `quarantine/codex/fleet-fits` with no push.

## Discovery through 12:23 UTC

Read-only inspection established the 960-cell P0 grid and separate 960-cell P2 grid in `accelerate_mlp.py`; the brief's 960 count describes one grid. At 12:10 UTC the potential lane's worklog reported P0 39/960 and P2 20/960, with both runners live. The later direct file count for the whole `fits/` tree (71) included non-MLP receipts; it must not be used as the P0 MLP count. `df -BG --output=avail /home /var/tmp` printed 68G available on `/home` and 1173G on `/var/tmp`. `jj git fetch` in zenmetrics exited 0 (`Nothing changed.`), then `jj workspace add ../zenmetrics--fleet-fits -r master@origin` exited 0 and based the workspace on `318a20f1`. No label values were read. The exploratory commands were read-only `cat`, `sed`, `rg`, `find`, `du`, `sha256sum`, `jj status` and `docker version` from the named repositories and scratch roots; their individual timing was not captured, so no fabricated timings are supplied.

At 12:23 UTC, `sha256sum` over the six potential scripts, folds, sampler preflight, bank snapshot receipt, and P2 peer manifest/verification exited 0. Its exact SHA-256 lines are transcribed in the preregistration committed before any replay. `ls -l` showed neither `FLEET_FITS_GO.md` nor `CODEX_QUOTA_STOP.md` existed. The user subsequently bound all new builds to current main/master of every resolved sibling crate, with clean snapshots and recorded commit IDs. This rule applies to every build in this lane.

## 12:24–12:42 UTC — current-main snapshots and code preparation

From the sibling repository directories, `jj git fetch` exited 0 (`Nothing changed.`) for `zensim`, `zenanalyze`, `zenresize`, `zenextras`, and `zenbench`; zenmetrics had been fetched earlier. `git rev-parse refs/remotes/origin/main` (master for zenmetrics) identified zensim `6a7b88e507ef6de666b592f0fc5ef600bc73538b`, zenmetrics `318a20f165920c494626659928c09cb4a8bc2cde`, zenanalyze `b102fa5f3480f0c3911fa85a8a6ad23c64936f18`, zenresize `e3975fb9d6d6b7baa96038a0eb8e27febb37c012`, zenextras `109a9ec367278a93751f7701f8536358e5d3f5cd`, and zenbench `53941021fd20158bb52af6654df003ca6ead9caa`. The local `Cargo.lock` git-source census showed only zenanalyze and zenmetrics pinned before those current-main commits; the other three pins already matched. No sibling working copy was edited or checked out.

At approximately 12:28 UTC, cwd `/home/lilith/work/zen/zenmetrics--fleet-fits`, `git -C /home/lilith/work/zen/zenmetrics archive 318a20f165920c494626659928c09cb4a8bc2cde | tar -x -C /var/tmp/fleet-fits/src/zenmetrics` exited 0; the four modified Rust files were copied over that clean base, `Cargo.deploy.toml` replaced `Cargo.toml` in the scratch snapshot, and its old lockfile was removed there. This makes the focused build resolve only zenfleet-core/ledger/ctl/worker from this repo, with no sibling paths or patches. Snapshot SHA-256 lines at that stage: core job `5daa66f34de3f44e06854249ef346cedd5883bbd92ef00e34a837f198505d386`; worker lib `b7142a7ede8456644bca81ac12bfcab08090cb323f05bcd014e8193bc8ad3475`; ctl lib `d5639c644ea28fc13f5ad99ea07fd9cdc0af5a0747f858e28d4547f6873585dc`; ctl bin `fdc811c7848bdce974020040534f1a197d790366aa5179fd3a433089b0b8c625`. Later source edits in the quarantine workspace are not yet in this first test snapshot and require an updated final test/build.

At 12:28:31 UTC, cwd `/var/tmp/fleet-fits/src/zenmetrics`, the command `/home/lilith/tmp/devin/heavy --mem 16G --jobs 8 -- bash -lc 'export CARGO_TARGET_DIR=/var/tmp/fleet-fits/target; cargo test -p zenfleet-core -p zenfleet-ctl -p zenfleet-worker --lib' > /var/tmp/fleet-fits/test1.log 2>&1` was queued (session 37343). As of 12:42 UTC its log was 0 bytes, so it had not acquired the shared heavy lock; there is no test verdict yet.

At approximately 12:38 UTC, cwd `/home/lilith/work/zen/zenmetrics--fleet-fits`, `git -C /home/lilith/work/zen/zensim archive 6a7b88e507ef6de666b592f0fc5ef600bc73538b | tar -x -C /var/tmp/fleet-fits/src/zensim` exited 0. The scratch snapshot's `Cargo.toml` changed only two git-rev values: zenanalyze to its current main `b102fa5f…` and zenmetrics/zenstats to current master `318a20f1…`; resulting manifest SHA-256 `98bd33c583d117fb0bb792fa7754a66cb801d5e18301868900afe5dfe60f9d46`. At ~12:38 UTC, cwd `/var/tmp/fleet-fits/src/zensim`, `/home/lilith/tmp/devin/heavy --mem 16G --jobs 8 -- bash -lc 'test ! -e /home/lilith/tmp/zensim-paper/rev4/CODEX_QUOTA_STOP.md || exit 75; export CARGO_TARGET_DIR=/var/tmp/fleet-fits/zensim-target; cargo build --target x86_64-unknown-linux-musl -p zensim-validate --bin zensim_mlp_train --bin bake_dial_refit --bin panel' > /var/tmp/fleet-fits/zensim-build.log 2>&1` was queued (session 45490), also 0 bytes at 12:42 UTC.

Light checks from the quarantine workspace: `PYTHONPYCACHEPREFIX=/var/tmp/fleet-fits/pycache python3 -m py_compile scripts/jobsys/*.py` for the four new fit scripts exited 0; `ruff check` on those four printed `All checks passed!`. A read-only comparison imported `build_fit_spec.grid()` and potential `accelerate_mlp.tasks('p0')`; actual output `960 960 0`, meaning identical destination sets and zero symmetric difference. `docker manifest inspect python:3.14.7-slim-bookworm >/dev/null` and `docker manifest inspect ghcr.io/imazen/zenfleet-worker:latest >/dev/null` both exited 0. Neither image has yet been built by this lane. The quota stop and GO files were absent at the 12:40 UTC check. The footprint manifest `/home/lilith/tmp/devin/rev4_fleet-fits_manifest.tsv` received 13 created/modified path rows at ~12:42 UTC.

## 12:52–12:55 UTC — frozen fit script era

The potential lane committed changes to `mlp_probe.py` and `linear_probe.py` after this lane's 12:23 hash pin. The former added a `FLEET_FITS_GO.md` guard so local P0 fit jobs stop after the handoff. That guard would also stop a fleet replay if the image copied the potential lane's live working tree. Read-only `jj file show -r ea8e8f6470a6` identified the committed, preregistered pre-guard script era; all four pinned script hashes matched the prereg (`4e7b18d0`, `2906e383`, `e80ec845`, `1deef7b7`), and `scripts/lib/zen_stats.py` was `6e2bed69`. Those five files were copied by `jj file show` into `/var/tmp/fleet-fits/program-src/` at ~12:54 UTC. This is a frozen script input, not a Rust crate dependency. All Rust builds remain current-main snapshots. The gate will still compare full predictions, scores and checkpoint bytes to dev originals. The gate verifier now returns failure for any required prediction, score, epoch or selected-checkpoint mismatch; the full result-file hash remains diagnostic as preregistered.

At 12:53 UTC the three queued heavy logs (`test1.log`, `zensim-build.log`, `data-gate-pack.log`) were still zero bytes because the shared heavy lock remained occupied by other lanes. `CODEX_QUOTA_STOP.md` and `FLEET_FITS_GO.md` were absent. Direct receipt count for the P0 MLP grid remained 39/960.

At ~12:59 UTC, cwd `/home/lilith/work/zen/zenmetrics--fleet-fits`, `PYTHONPYCACHEPREFIX=/var/tmp/fleet-fits/pycache python3 -m py_compile scripts/jobsys/{build_fit_spec,fit_cell_exec,harvest_fit_cells,pack_fit_data,pack_fit_program,verify_fit_gate}.py; ruff check scripts/jobsys/{build_fit_spec,fit_cell_exec,harvest_fit_cells,pack_fit_data,pack_fit_program,verify_fit_gate}.py` exited 0, with `All checks passed!`. `bash -n scripts/jobsys/build_executor_image.sh; rustfmt --check --edition 2024 crates/zenfleet-core/src/job.rs crates/zenfleet-ctl/src/lib.rs crates/zenfleet-ctl/src/bin/jobctl.rs crates/zenfleet-worker/src/lib.rs` exited 0 with no output. The fit cost was set to the brief's measured ~400 s rather than the generic RAM-derived 81 s estimate, yielding a 20× watchdog allowance; this was not yet run in a worker. The queued focused test snapshot was updated before acquiring the heavy lock: `job.rs` SHA-256 `6b423bc2e8022e629c02608248f3440c8d4b87082341fa9c5932c793d96caec4`, `ctl/lib.rs` `1f2e440731ac3b26de50e521dfa0446824ac16baded0154de8b910bcee156511`. The test log was still zero bytes at that check, so there is no Rust pass claim yet.

At ~13:00 UTC, cwd the zenmetrics quarantine workspace, `jj commit -m ...` created `8b0caa9e265c883813f4f932fa19b999ce12f63d` for the staged fit support and the prereg script-era amendment; `jj bookmark set quarantine/codex/fleet-fits -r @-` moved only this lane's local bookmark. No push. The changelog now names that implementation commit. A second `jj git fetch` across zenmetrics, zensim, zenanalyze, zenresize, zenextras and zenbench reported `Nothing changed.` in each repository. At 13:02 UTC the queued logs were still empty and no Rust runtime pass was claimed.

## 13:04–13:13 UTC — static packaging audit while lock is busy

The two selected TRAIN pilot originals exist: `POT_kadid_train_r0_mlp32/o0_r0/result.json` SHA-256 `dfc9f7a3cdfac871c2de320dbcb2120fc9a4a772d445eddf4587519b790f6456`, and `.../o1_r1/result.json` `21034eadbbf6abf4c0db64bacef0037ee05ac634a0e0e77fdb3d2fd91b659f35`. `rustup target list --installed` includes `x86_64-unknown-linux-musl`; `command -v musl-gcc` printed `/usr/bin/musl-gcc`. `/home` had 65G and `/var/tmp` 1168G available at ~13:10 UTC. No pilot was run yet.

`just fleet-check` exited 0 and printed `canonical=17 warnings=0 errors=0` and `PASS`; `just hygiene-check` exited 0 after its self-test and printed `2295 tracked files checked against 12 pattern(s) (9 site-specific), no address/identifier hits`. The image recipe now installs its entrypoint's system tools at build time and its build helper checks them. `bash -n scripts/jobsys/build_executor_image.sh` exited 0. `ruff check` and `python3 -m py_compile` on the revised packers and gate verifier exited 0 (`All checks passed!`). The packers use an empty gzip header filename for byte-stable archives. The verifier's pass criterion now excludes the whole-result-file hash, which the prereg treats as diagnostic, while requiring exact prediction, score, epoch and selected-checkpoint equality.

Read-only inspection of the potential lane's exposure ledger identified target-free admitted-table receipt `/var/tmp/rev4-featpot/admit_bank.jsonl` SHA-256 `aaf927dc2a8c8dd6066ce740bef53f137a61cee8e26bb3210d440d38465cdbcb`; `sha256sum` reproduced that line. The packer now requires this receipt and verifies every selected admitted Parquet against its recorded `output_sha256`. This input pin was added to the prereg before any replay. At 13:13 UTC the three queued heavy logs still had zero bytes; the shared lock had moved to another potential-lane batch. `CODEX_QUOTA_STOP.md` and `FLEET_FITS_GO.md` were absent.

At ~13:17 UTC, read-only inspection of `fleet-entrypoint.sh` and `tmpdir_discipline.sh` found `findmnt` in the boot preflight. The fit image now installs `util-linux` at build time and its image smoke checks `findmnt` along with `aws`, `s5cmd`, `zenfleet-worker`, `gunzip` and `timeout`; no image has been built yet. `bash -n scripts/jobsys/build_executor_image.sh` exited 0. At 13:21 UTC, `test1.log`, `zensim-build.log` and `data-gate-pack.log` were all still zero bytes while the potential lane held the shared heavy lock. No quota stop or fleet GO file existed.

## 13:25–13:30 UTC — job identity closure

`jj git fetch` in the same six source repositories again returned `Nothing changed.` at ~13:25 UTC. `rustc --version` printed `rustc 1.98.1 (48a229cea 2026-09-01)` and `cargo --version` printed `cargo 1.98.1 (797e8a9bc 2026-08-05)`. Read-only review found that the first image recipe installed `fit_cell_exec.py` separately from the hashed program tar, so executor changes could have reused job IDs. Before any program bundle or replay, the packer now includes the executor script; the Dockerfile installs the executor from the verified archive. `ruff check scripts/jobsys/pack_fit_program.py` printed `All checks passed!`, `python3 -m py_compile` and `bash -n scripts/jobsys/build_executor_image.sh` exited 0. The corresponding prereg clarification was committed before any replay. The three heavy logs remained zero bytes at 13:30 UTC.

At ~13:37 UTC, read-only `ssh` and `df -BG --output=avail` on the two permitted workers returned `20080G` for the capped host's scratch and `101G` for the second worker's scratch filesystem (both exit 0). `du -sh` on two completed TRAIN fit directories returned `118M` each; this is a raw per-cell size, not an output-blob measurement. The second worker's scratch use must be watched during the run. No host was launched or changed. The P0 result-file count was 40/960 while the potential lane's batch held the heavy lock; that count is not frozen for the later GO handoff.

The 118M raw fit directory led to one executor change before any replay: after assembling the complete tar output in memory, it removes that cell's scratch directory, keeping only the verified shared input archive. The zenfleet worker still owns blob persistence and ledger writes; if an upload fails, it may rerun the deterministic cell. `python3 -m py_compile` and `ruff check scripts/jobsys/fit_cell_exec.py` exited 0 (`All checks passed!`); the revised executor SHA-256 is `6acf5f1fea3e7d6bf6ef507c3935f002c3c45218d3d05f86af3705cb3786e0a9`. No runtime or memory peak is claimed yet.

## 13:44–13:48 UTC — first current-main build gates

The queued focused Rust test command from `/var/tmp/fleet-fits/src/zenmetrics` completed at approximately 13:44 UTC, exit 0. Its output `/var/tmp/fleet-fits/test1.log` has SHA-256 `03bfa7c4aa7db675c1091c1b8b368746e4e4437630893253b11cd387f99f146b`; exact summary lines: `test result: ok. 145 passed; 0 failed` for core, `27 passed; 0 failed` for ctl, and `41 passed; 0 failed` for worker. The final wrapper line reported `run-heavy: done rc=0 27s | peak-RSS 0.90GiB | min-avail 17170MiB | peak-load 6.78`. The tested scratch Rust files match the quarantine workspace byte-for-byte (`cmp` exit 0 for the four modified Rust files). This includes the new fit declaration identity test and the existing golden job-ID tests; it does not yet prove runtime fit parity.

The queued zensim command from `/var/tmp/fleet-fits/src/zensim` completed at approximately 13:46 UTC, exit 0. The log `/var/tmp/fleet-fits/zensim-build.log` has SHA-256 `49a4f0ca96d19b9475c6a3feb66c150adb8896fd8d0bd14e6589be6f88a7fe01`; exact final lines: `Finished dev profile [optimized + debuginfo] target(s) in 1m 40s` and `run-heavy: done rc=0 100s | peak-RSS 1.08GiB | min-avail 11905MiB | peak-load 13.93`. The three resulting musl binaries are statically linked and unstripped. The gate data command from the zenmetrics quarantine workspace, `/home/lilith/tmp/devin/heavy --mem 4G --jobs 1 -- bash -lc 'test ! -e /home/lilith/tmp/zensim-paper/rev4/CODEX_QUOTA_STOP.md || exit 75; PYTHONPYCACHEPREFIX=/var/tmp/fleet-fits/pycache python3 scripts/jobsys/pack_fit_data.py --sets kadid_train --out /var/tmp/fleet-fits/data-gate.tar.gz' > /var/tmp/fleet-fits/data-gate-pack.log 2>&1`, completed at approximately 13:46 UTC, exit 0. Its exact JSON reports `train_only: true`, `files: 16`, `archive_bytes: 32762764`, SHA-256 `59b47d0c827e7de4118d981a4b06293f0a701b25b130bf35fef9b6e7fa904a6a`. No held-out rows were copied for this pilot archive.

At ~13:47 UTC, `jj git fetch` in zenmetrics, zensim, zenanalyze, zenresize, zenextras and zenbench returned `Nothing changed.` in each repo. The zensim scratch lock has 585 package records and exactly five git source URLs, all pinned to the just-fetched main/master commits above; the zenmetrics scratch lock has 294 package records and no git source. The source archive and Cargo locks contain no dirty sibling working-copy paths. `/home` available space printed `49G` at ~13:48 UTC, above the lane's 20G floor. A musl release build of `zenfleet-worker` and `zenfleet-ctl` was queued under `heavy` from `/var/tmp/fleet-fits/src/zenmetrics`; its result is still pending.

## 13:49–13:56 UTC — source graph audit and fleet-era decision

From both clean source snapshots, `CARGO_TARGET_DIR=/var/tmp/fleet-fits/{zensim-target,target} cargo metadata --locked --offline --format-version 1` exited 0 and wrote `/var/tmp/fleet-fits/{zensim,zenmetrics}-metadata.json`. A Python read of `packages[*].manifest_path` for every `source == null` package found zero paths outside either clean snapshot (zensim: 326 resolved packages; zenmetrics: 294). No `zenavif` package was in either resolved graph; its dirty checkout was never read for a build. `write_fit_build_meta.py` audited the lock records against current main heads and wrote `/var/tmp/fleet-fits/build_meta.json`, SHA-256 `c6d9e32e95c3e14ba90707bee4e83fcb630676bcc67638bd4dbb055cf200c7bc`, with 585 zensim and 294 zenmetrics lock package records, five git heads. Registry packages are pinned by version and lock checksum; they do not have a repository commit ID in Cargo's source model. This metadata must be rechecked after the zenfleet release build and before the image bundle.

At ~13:51 UTC, in the zensim quarantine workspace, `jj commit` created the D1 exposure-ledger entry in `docs/DATA_SPLITS.md` before the full archive was requested. The ledger's SHA-256 was `df8eb98ae158e59761dae5a3985c5805149d149bf2052c28d74e1a82d6f25a7b`; its local bookmark is `quarantine/codex/fleet-fits` there. The command to pack all eight P0 sets under `heavy` was queued after that commit; the result is pending. Both allowed hosts returned `docker-ready-scratch-*` from a read-only Docker/scratch check, exit 0; the role names are suppressed here under the privacy rule.

Read-only inspection of an original TRAIN cell's `inner0/train.log` at ~13:55 UTC showed `t=34.3s`, `t=40.8s` and other elapsed-time values in logged epoch lines. Its `result.json` embeds `inner[*].log_sha256`, so whole-file JSON byte identity across hosts is not a meaningful deterministic outcome test. Before any replay, the gate verifier was tightened to require exact predictions, all scores, selected epoch and checkpoint bytes, inner fold counts/bounds/score series, final bounds/rows/indices, seeds and source provenance. It excludes only the timing-bearing log SHA from the inner comparison and still reports full JSON hashes. A dated prereg amendment now commits this interpretation and the conservative decision to run **all 960 P0 cells in one fleet era**, including dev-completed cells. No old dev results will enter the final grid. Host-host outcome differences still block READY until diagnosed. The potential lane's `mlp_probe.py` and statistical code are untouched.

At ~14:08 UTC, the shared `heavy` lock still held the queued zenfleet release build, full data pack and strip task at zero-byte logs. A private pilot transport helper `/var/tmp/fleet-fits/pilot_gate_transport.sh` (SHA-256 `877ceddcbaa2de37f4a05031fcc7975bacc4a8530b2186ee4bf4365f7a222f47`) was written and passed `bash -n`; it accepts host aliases as arguments rather than storing them in tracked source, applies the required Docker caps to the capped role, and collects the zenfleet ledger and blobs. No remote container or image transfer has occurred. Read-only `nproc` checks returned 32 CPUs on the capped host before the 24-CPU Docker cpuset and 6 CPUs on the second host; at the registered 4-thread hint, ideal parallelism is roughly seven cells and 960×400/7 ≈ 15.2 h before contention and transport. This is a planning calculation, not a measured runtime.

At ~14:09 UTC, `lan_score_launch.sh` was modified in this quarantine lane to forward an optional `ZEN_MAX_MIN` to its remote container, defaulting to its prior 1400 minutes. This allows the fixed P0 manifest's budget to exceed the measured full-run estimate without changing existing score jobs. `bash -n scripts/jobsys/lan_score_launch.sh` exited 0; no launch has occurred.

## HANDOFF — 2026-09-24 14:12 UTC, quota stop

The user issued QUOTA STOP at 68.0% weekly usage, and `/home/lilith/tmp/zensim-paper/rev4/CODEX_QUOTA_STOP.md` is present. At 14:12 UTC, the three queued heavy sessions were interrupted with Ctrl-C: zenfleet musl release build session 41589, full eight-set data pack session 77842, and stripped-binary copy session 47402. All returned exit code 130. Their logs `/var/tmp/fleet-fits/{zenfleet-build,data-full-pack,strip-bin}.log` are zero bytes; no task acquired the shared heavy lock or produced an output. No image was built or pushed, no host received an image, no pilot or full fit was run, and no fleet manifest was uploaded. `FLEET_FITS_READY.md`, `FLEET_FITS_GO.md`, `FLEET_FITS_RUN.md`, and `FLEET_FITS_DONE.md` were absent at stop. Do not infer a determinism pass.

Completed, reviewable state: the zenmetrics quarantine workspace contains the `FitCell` job kind, `declare-fits`, image and executor recipes, content-addressed data/program packers, expanded gate verifier, harvest verifier, preregistration, docs and this worklog; latest bookmark before this HANDOFF is `quarantine/codex/fleet-fits` at `747faa1d22d76b9156e84bb843bd3542ee978b66`, with a clean working copy. The zensim quarantine workspace has a D1 exposure-ledger entry committed at `d2cb3b2f7b66bee4af8575033346c5c2a2316c6e`, also on its own `quarantine/codex/fleet-fits` bookmark. No push. The focused current-main Rust tests passed 145+27+41 tests; current-main zensim's three musl debug binaries built successfully but remain unstripped at `/var/tmp/fleet-fits/zensim-target/x86_64-unknown-linux-musl/debug/`. The TRAIN-only gate archive is `/var/tmp/fleet-fits/data-gate.tar.gz`, SHA-256 `59b47d0c827e7de4118d981a4b06293f0a701b25b130bf35fef9b6e7fa904a6a`. The audited initial build metadata is `/var/tmp/fleet-fits/build_meta.json`, SHA-256 `c6d9e32e95c3e14ba90707bee4e83fcb630676bcc67638bd4dbb055cf200c7bc`. It should be regenerated after the release build. Current-main source snapshots are under `/var/tmp/fleet-fits/src/`; their Cargo metadata audit found zero local crate paths outside those snapshots. The `zenavif` checkout was never used by these builds. A private pilot transport helper is `/var/tmp/fleet-fits/pilot_gate_transport.sh`, SHA-256 `877ceddcbaa2de37f4a05031fcc7975bacc4a8530b2186ee4bf4365f7a222f47`, syntax checked but not executed.

Exact resume sequence, only after the quota stop is lifted: (1) confirm `/home` ≥20 GB and re-run `jj git fetch` in zenmetrics, zensim, zenanalyze, zenresize, zenextras and zenbench; compare fetched heads with the recorded commits, and rebuild any source snapshot whose main moved before using its binaries. (2) Through `/home/lilith/tmp/devin/heavy`, run the interrupted zenfleet release command from `/var/tmp/fleet-fits/src/zenmetrics`: `CARGO_TARGET_DIR=/var/tmp/fleet-fits/target cargo build --release --target x86_64-unknown-linux-musl -p zenfleet-worker --bin zenfleet-worker -p zenfleet-ctl --bin zenfleet-ctl`. Run the queued `pack_fit_data.py --sets kadid_train,tid2013,konfig_train,konjnd_bpg_train,cid22_a25,aic3,kadid_select,konfig_val --out /var/tmp/fleet-fits/data-full.tar.gz`, then copy and strip the three clean zensim binaries to `/var/tmp/fleet-fits/bin/` through `heavy`. (3) Recheck both Cargo locks and regenerate `build_meta.json`; pack the frozen preregistered scripts, exact executor, three stripped Rust binaries and metadata with `pack_fit_program.py`. Build a new canonical `zenfleet-worker` fit tag locally with `FIT_CELL_IMAGE=1` and `PUSH=0`; record its local digest. (4) Declare the two already-completed TRAIN cells with the new `zenfleet-ctl`, stage the local image and TRAIN archive on both allowed workers, run the zenfleet pilot with the capped role's exact Docker limits, collect ledger/blobs and execute `verify_fit_gate.py`. Diagnose any host-host outcome difference. The preregistration already commits to a single current-main era: all 960 P0 cells are rerun, even if dev-vs-fleet outcomes match, because `result.json` embeds timing-bearing log hashes. Record gate result and decision in this worklog and prereg amendment. (5) Write `FLEET_FITS_READY.md` with commits, image tag/digest, gate result, full 960-cell manifest/input hashes/upload plan and measured wall estimate. Wait for coordinator-written `FLEET_FITS_GO.md`, polling every 10 minutes. After GO, follow the brief: wait for the potential lane's pause handoff, push image, upload content-addressed inputs, declare jobs, write RUN, launch only permitted workers, monitor, verify all receipts, harvest, tear down containers, and write DONE with MISSING first. Never push the source bookmarks.

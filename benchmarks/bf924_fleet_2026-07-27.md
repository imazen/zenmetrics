# bf924 fleet — 924-feature backfill measurements (2026-07-27)

Config: zenmetrics `2095f80b`, zensim `0b3d16b0`, image `exec-zensim924-2095f80b`,
metric `zensim-foldapp` (924 feats, features-only), 490,173 cells / ~5.74M variants
across 54 runs, pool `_pool924`. Household fleet only (no paid cloud).

## Smoke (wsl, 2026-07-27 ~11:15–11:19Z)

4-min bounded `zenfleet-worker` pass on `bf924-zwebp-t2`, ZEN_CORE_OVERSUBSCRIBE=2
(56 exec slots on 28 cores), RAYON_NUM_THREADS=1, R2-direct (no tower staging):

- **2,791 cell blobs landed ≈ 11.6 cells/s ≈ ~134 variant-rows/s** on a SHARED wsl
  (concurrent claude sessions + a fuzz s5cmd sync).
- Verified blob content: 12/12 rows kind=feature, regime=folded720append,
  **924 features, all finite**, 0 error rows.
- Worker self-reported budget: 44.1 GiB / 56 cores; RSS steady ~1.4 GiB.

## Fleet bring-up (first hour)

| box | start | config | first-minute stats |
|---|---|---|---|
| tower `zen924-basement` | 11:21:54Z | cpuset 0–23, shares 256, mem 40g, oversub 3 | cpu 2230%, mem 1.5 GiB |
| lianli | 11:23:05Z | uncapped (dedicated), oversub 3 | cpu 639% (ramping), mem 1.4 GiB |
| jason (zen-node-2) | ~11:26Z | PXE-node unit, oversub 3 | enrolled+active |

Instrumentation follow-ups land below as they are measured (per-box cells/s from
ledger growth, fetch-vs-compute split, oversubscription tuning, R2-direct vs
tower-staged fetch).

## Live samples

| t (UTC) | total done cells | fleet rate | tower CPU | lianli CPU | jason CPU |
|---|---|---|---|---|---|
| 11:30:46 | ~10,100 (incl. 2,791 smoke) | — | — | — | — |
| 11:34:54 | **19,902 / 490,173 (4.1%)** | ~22 cells/s (13-min window, 3 boxes) | 2051% | 188% | 1399% |

- lianli pass 1: bf924-zjxll-t14 done=5076 in ~6 min ≈ 14 cells/s solo.
- Between-run transitions show as brief CPU dips (manifest+snapshot fetch) — normal round-robin.
- OPEN: lianli sampled low-CPU twice while on an avif run — watch whether avif-run
  phases are fetch-bound (larger variants / slower range-GETs); if sustained across
  boxes, raise ZEN_CORE_OVERSUBSCRIBE or flip ZEN_VARIANTS_LOCAL_DIR to the tower
  stage (57+ GB of 188 GB TARs already mirrored by 11:31).
- Error rows: 0 on tower after first 3 runs; smoke blob audit 0 errors.

| 11:52:49 | 42,651 (8.7%) | 21.2 cells/s | — | — | — |
| 12:05:53 | **58,705 (12.0%)** | 20.4 cells/s | — | — | — |

Sustained ~20–22 cells/s on 3 boxes across 3 samples; CPU-saturated (tower
2000%+, jason 1400–1570%) — R2-direct fetch is NOT the bottleneck at
oversub=3. Tower staging: TARs (176G) + refs complete; zjl2 encodes syncing.

## mac worker RCA (fixed 2026-07-27)

lilith-mac cycled all 54 runs at done=0 (skipped=all) under launchd while
login-shell runs scored. Root cause: `claim_or_steal_r2_key` SPAWNS the
`aws` CLI (the claim CAS was never ported to native s3io); brew's aws is
not on launchd's minimal PATH and the payload doesn't bundle it → every
claim spawn failed instantly → skip-all. Also fixed on the way: darwin RAM
detection (`/proc/meminfo` absent → 2 GiB floor → 1.5 GiB admission budget;
now `sysctl hw.memsize` + `ZEN_RAM_BYTES` override). Follow-up: port the
claim CAS to native s3io (kills the aws-CLI spawn cost everywhere).

## 4-box steady state (sampler series, total done cells)

| t (UTC) | total | interval rate |
|---|---|---|
| 12:17 | 70,965 | — |
| 12:38 | 98,030 | 21.3 c/s |
| 12:59 | 123,834 | 20.0 c/s |
| 13:21 | 155,032 | 23.9 c/s |
| 13:43 | 183,547 | 21.6 c/s |
| 14:06 | **214,365 (43.7%)** | 22.9 c/s |

Fleet = tower + lianli + jason + mac (mac joined ~12:15 after the two darwin
fixes; its first pass: done=4650 on zjxll-t23). Sustained ~21–24 cells/s ⇒
projected drain ~17:30Z at this composition (observed-rate projection, not a
measured completion). node-4 + ian still pending physical intervention.

## COMPLETION (2026-07-27 ~18:58Z)

**Reconcile verdict: COMPLETE — gap==0 on all 54 runs.** distinct_done
490,173/490,173, failed-only 0, raw ledger rows 661,139 ⇒ **measured re-score
tax 1.35×** (vs the 2.0–3.4× no-snapshot baseline — the 30-min snapshot cron
earned its keep). Wall: first worker 11:21:54Z → drain ~18:55Z ≈ **7.5 h** on
tower+lianli+jason+mac (jason joined 11:26, mac 12:15 after its two darwin
fixes). $0 spent. Tooling: `scripts/jobsys/pool_reconcile_report.py` (the
distinct-done-vs-manifest accounting; also caught that blob-counts and
snapshot-row sums both overcount).

Shutdown: all four workers stopped post-drain; jason flipped back to Windows;
tower refresh cron removed (no drained-pool cyclers left behind). node-4 + ian
never joined (hardware-gated) and were not needed.

# Dense picker corpus 2026-06-25 (pointer)

Size-dense, tier-balanced rendition corpus generated by `scripts/picker/gen_dense_corpus.py`
to fix the small/medium starvation that capped the lossy_dense picker at 8.54% val
(prior pool: 86% tiny, only 26 small + 29 medium renditions).

- **Location (block storage):** `/mnt/v/output/dense-corpus-2026-06-25/`
- **Regenerable** (deterministic): `gen_dense_corpus.py --src /mnt/v/output/imazen-26-features/train_renditions_2026-06-14 --out /mnt/v/output/dense-corpus-2026-06-25`
- **4412 renditions** from 1082 sources (downscaled, Lanczos, no upscaling). Tier balance:
  - distinct SOURCES/tier: tiny 1082 / small 150 / medium 149 / large 139
  - renditions/tier: tiny 3397 / small 300 / medium 437 / large 278
- **Provenance:** `_provenance.tsv` (rendition → source_file + source_sha256 + WxH + tier), sha256 `b75849374d5fbd2b1e9ce6963c8138fe79d7a2be76e67d11a26153aaeba79995`

## Remaining chain to the ≤1% picker (next session)
1. **Subsample tiny** to ~150 sources (balance the sweep; tiny is over-represented).
2. **Extract imazen26 content features:** `cargo run --release --example extract_features_for_picker`
   (zenanalyze) over the corpus → features TSV (the picker input; the existing
   imazen26_train_features TSV does NOT cover these new sizes).
3. **Sweep as SPLIT** (encode-once / score-many) — encode on Hetzner CPU
   (`hetzner_cpu_sweep.sh`, SWEEP_JOBS bounded for ≤1MP; job system for >1MP per the
   heaptrack OOM finding), then `score-pairs` (now ref-amortized, −35%) for metrics.
   codec-corpus RO / zentrain RW. `fleet watch`; kill idle boxes; stay under $25.
4. **Retrain:** `omni_to_pareto.py` → `train_hybrid --codec-config zenjxl_lossy_dense` →
   oracle gap. Expect the DATA_STARVED/PER_SIZE_TAIL gates to clear with the balanced tiers.
5. **Iterate** to grid-stable (no new axis two rounds) + **code settled knobs** into jxl crates.

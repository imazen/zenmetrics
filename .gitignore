*.bc
.claude/
/data
*.deb
/frames
/.idea
*.ncu-rep
*.o
*.ptx
*.rlib
/target
**/target
.workongoing
.DS_Store

# Python venvs and local outputs from scripts/cvvdp_goldens/.
# Goldens themselves live on R2 (coefficient bucket), not in-tree.
**/.venv/
**/__pycache__/
*.pyc
scripts/cvvdp_goldens/out/

# Repo-root staging for Dockerfile.sweep.vNN precompiled-binary builds.
# The CI workflow downloads the artifact to this path; local builds
# `cp target/release/zen-metrics ./zen-metrics-vNN`. Never commit it.
/zen-metrics
/zen-metrics-v*
# Anchor to root: the docker-build artifact at /vastai-fleet, NOT
# the source directory crates/vastai-fleet/.
/vastai-fleet
/zen-metrics
